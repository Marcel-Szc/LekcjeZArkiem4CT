<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">
    <ListView x:Name="MojaLista"
              BackgroundColor="Bisque" 
              SeparatorColor="Aqua"
              ItemSelected="ListaWybrana"
              >
        <ListView.Header>
            <StackLayout>
                <Label Text="To jest człowieku lista" TextColor="Black"/>
            </StackLayout>
        </ListView.Header>
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <HorizontalStackLayout Spacing="5">
                        <Label Text="{Binding Imie}" TextColor="Black"/>
                        <Label Text="{Binding Nazwisko}" TextColor="Black"/>
                    </HorizontalStackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
