<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Menu.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Menu"
    Title="Menu">

    <!-- O NAS -->
    <FlyoutItem Title="O nas">
        <ShellContent ContentTemplate="{DataTemplate local:ONasPage}" />
    </FlyoutItem>

    <!-- GALERIA -->
    <FlyoutItem Title="Galeria">
        <ShellContent ContentTemplate="{DataTemplate local:GaleriaPage}" />
    </FlyoutItem>

    <!-- KONTAKT -->
    <FlyoutItem Title="Kontakt">
        <ShellContent ContentTemplate="{DataTemplate local:KontaktPage}" />
    </FlyoutItem>

</Shell>